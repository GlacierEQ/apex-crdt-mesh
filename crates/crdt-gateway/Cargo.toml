[package]
name = "crdt-gateway"
version.workspace = true
edition.workspace = true

[dependencies]
crdt-core = { path = "../crdt-core" }
crdt-rpc = { path = "../crdt-rpc" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
thiserror = "1"

[package]
name = "crdt-rpc"
version.workspace = true
edition.workspace = true

[dependencies]
crdt-core = { path = "../crdt-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
